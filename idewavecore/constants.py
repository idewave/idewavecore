from os import path


# use for files import
ROOT_DIR = path.dirname(path.abspath(__file__))

# use for modules import
PACKAGE_ROOT = 'idewavecore'

# middlewares building data
MIDDLEWARES_DIR = 'middlewares'
NATIVE_MIDDLEWARES_PREFIX = 'native'
